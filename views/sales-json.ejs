<%- include('parts/html_head') %>
    <%- include('parts/html_navbar') %>


        <div class="container">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">姓名</th>
                        <th scope="col">年齡</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(items of sales){ %>
                        <tr>
                            <td>
                                <%= items.number %>
                            </td>
                            <td>
                                <%= items.name %>
                            </td>
                            <td>
                                <%= items.age %>
                            </td>
                        </tr>
                        <% } %>
                </tbody>
            </table>
        </div>


        <%- include('parts/scripts') %>
            <%- include('parts/html_foot') %>